{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "rootDirectory": "/premium-dashboard"
  },
  "deploy": {
    "startCommand": "npm run start -- --port ${PORT:-3000} --hostname 0.0.0.0",
    "restartPolicyType": "ON_FAILURE"
  }
}
